//import {WebsocketService} from './websocket.service';

export function main() {
  describe('Chat Service', () => {
    let websocketService: string;

    beforeEach(() => {
      websocketService = '';
    });

    it('should test something', () => {
      expect(websocketService).not.toBeNull();
    });
  });
}
