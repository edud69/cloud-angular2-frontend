//import {WebsocketHandler} from './websocketHandler.service';

export function main() {
  describe('WebsocketHandler Service', () => {
    let websocketHandlerService: string;

    beforeEach(() => {
      websocketHandlerService = '';
    });

    it('should test something', () => {
      expect(websocketHandlerService).not.toBeNull();
    });
  });
}
