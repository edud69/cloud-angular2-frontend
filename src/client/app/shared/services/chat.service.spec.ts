//import {ChatService} from './chat.service';

export function main() {
  describe('Chat Service', () => {
    let chatService: string;

    beforeEach(() => {
      chatService = '';
    });

    it('should test something', () => {
      expect(chatService).not.toBeNull();
    });
  });
}
